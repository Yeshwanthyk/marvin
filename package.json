{
  "name": "mu-agent",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "tsc --noEmit -p packages/types/tsconfig.json && tsc --noEmit -p packages/tools/tsconfig.json && tsc --noEmit -p packages/providers/tsconfig.json && tsc --noEmit -p packages/runtime/tsconfig.json"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@sinclair/typebox": "^0.32.33"
  },
  "devDependencies": {
    "@types/node": "^25.0.1",
    "typescript": "^5.5.4"
  }
}
