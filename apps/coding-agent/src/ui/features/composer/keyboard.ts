import { createKeyboardHandler, type KeyboardHandlerConfig } from "../../../keyboard-handler.js"

export const createComposerKeyboardHandler = (config: KeyboardHandlerConfig) => createKeyboardHandler(config)
