# Extension Examples

These examples are Pi-compatible Marvin extensions. Load any file or package directory with:

```bash
marvin -e ./apps/coding-agent/examples/extensions/hello.ts
```

For authoring details, read `apps/coding-agent/docs/extensions.md` and `apps/coding-agent/docs/packages.md`.
